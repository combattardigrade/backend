extends layout.pug

block header
    include header

block content   
    .container-fuild
        .row
            div(class='col-10 offset-1')
                h3.mt-4 Datos generales
                .table-responsive
                    table(class='table table-bordered table-striped ') 
                        thead
                            th Dato
                            th Total
                            th
                        tbody
                            tr
                                td Usuarios
                                if data.users
                                    td #{data.users.count}
                                else
                                    td 0
                                td.tableBtn 
                                    a(class='btn btn-primary' href=host + '/admin/users') Ver
                            tr
                                td Scooters
                                if data.scooters
                                    td #{data.scooters.count}
                                else
                                    td 0
                                td.tableBtn 
                                    a(class='btn btn-primary' href=host + '/admin/scooters/all?page=1') Ver                      
                            tr
                                td Órdenes
                                if data.orders
                                    td #{data.orders.count}
                                else
                                    td 0
                                td.tableBtn 
                                    a(class='btn btn-primary' href=host + '/admin/orders') Ver   
